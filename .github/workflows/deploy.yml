name: Deploy

on:
  push:
    branches: [main]
  workflow_dispatch: # Allow manual trigger

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    steps:
      - name: 🚀 Deploy LaneGap
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.HOST }}
          username: ${{ secrets.USERNAME }}
          password: ${{ secrets.PASSWORD }}
          port: 22
          script: |
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🚀 LaneGap - Production Deploy"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""

            # Navigate to project
            cd teczer-deployement/LaneGap
            echo "📂 Current directory: $(pwd)"
            echo ""

            # Pull latest changes
            echo "⬇️  Pulling latest changes from main..."
            sudo git pull origin main
            echo "✅ Git pull completed"
            echo ""

            # Stop running containers
            echo "🛑 Stopping running containers..."
            sudo docker compose down
            echo "✅ Containers stopped"
            echo ""

            # Clean old images
            echo "🧹 Cleaning old Docker images..."
            sudo docker system prune -f
            echo "✅ Cleanup completed"
            echo ""

            # Build new images
            echo "🔨 Building Docker images..."
            sudo docker compose build
            echo "✅ Build completed"
            echo ""

            # Start containers
            echo "▶️  Starting containers..."
            sudo docker compose up -d
            echo "✅ Containers started"
            echo ""

            # Wait for services to be ready
            echo "⏳ Waiting for services to be ready (20s)..."
            sleep 20
            echo ""

            # Health checks
            echo "🏥 Running health checks..."

            PB_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:2123/api/health 2>/dev/null || echo "000")
            if [ "$PB_STATUS" = "200" ]; then
              echo "✅ PocketBase Health: OK (HTTP 200)"
            else
              echo "❌ PocketBase Health: FAILED (HTTP $PB_STATUS)"
            fi

            WEB_STATUS=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:2124 2>/dev/null || echo "000")
            if [ "$WEB_STATUS" = "200" ]; then
              echo "✅ Web Health: OK (HTTP 200)"
            else
              echo "❌ Web Health: FAILED (HTTP $WEB_STATUS)"
            fi

            echo ""
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "📊 Services Status"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            sudo docker compose ps
            echo ""

            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "🎉 Deployment Complete!"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo ""
            echo "📊 Services:"
            echo "   • Frontend:   http://localhost:2124"
            echo "   • PocketBase: http://localhost:2123"
            echo "   • PB Admin:   http://localhost:2123/_/"
            echo ""
            echo "📝 Useful Commands:"
            echo "   • View logs:    sudo docker compose logs -f"
            echo "   • Restart:      sudo docker compose restart"
            echo "   • Stop:         sudo docker compose down"
            echo ""
